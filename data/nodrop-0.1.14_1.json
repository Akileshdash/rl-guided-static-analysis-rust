{
    "level": "Warning",
    "analyzer": "UnsafeDestructor",
    "op_type": null,
    "description": "unsafe block detected in drop",
    "file": "nodrop-0.1.14/src/lib.rs",
    "start_line": 84,
    "start_col": 5,
    "end_line": 84,
    "end_col": 31,
    "code_snippet": "impl<T> Drop for NoDrop<T> {\n        fn drop(&mut self) {\n            if needs_drop::<T>() {\n                // inhibit drop\n                unsafe {\n                    ptr::write(&mut self.0, Flag::Dropped(0));\n                }\n            }\n        }\n    }"
}