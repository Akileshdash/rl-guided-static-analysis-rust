{
    "level": "Warning",
    "analyzer": "UnsafeDestructor",
    "op_type": null,
    "description": "unsafe block detected in drop",
    "file": "shipyard-0.8.1/src/public_transport.rs",
    "start_line": 84,
    "start_col": 1,
    "end_line": 84,
    "end_col": 35,
    "code_snippet": "impl<T> Drop for WriteGuard<'_, T> {\n    fn drop(&mut self) {\n        match self {\n            Self::Custom { lock, .. } => unsafe {\n                lock.unlock_exclusive();\n            },\n            #[cfg(feature = \"std\")]\n            Self::Std { .. } => {}\n        }\n    }\n}"
}