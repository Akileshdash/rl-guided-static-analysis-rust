{
    "level": "Warning",
    "analyzer": "UnsafeDestructor",
    "op_type": null,
    "description": "unsafe block detected in drop",
    "file": "gluon_vm-0.18.2/src/gc/mutex.rs",
    "start_line": 170,
    "start_col": 1,
    "end_line": 172,
    "end_col": 23,
    "code_snippet": "impl<'a, T> Drop for MutexGuard<'a, T>\nwhere\n    T: ?Sized + Trace,\n{\n    fn drop(&mut self) {\n        let rooted = self.rooted.lock().unwrap();\n        if !*rooted {\n            unsafe {\n                self.value.unroot();\n            }\n        }\n    }\n}"
}