{
    "level": "Info",
    "analyzer": "UnsafeDataflow",
    "op_type": "Transmute",
    "description": "Potential unsafe dataflow issue in `<W as Packer>::pack`",
    "file": "bytepack-0.4.1/src/lib.rs",
    "start_line": 1039,
    "start_col": 5,
    "end_line": 1045,
    "end_col": 6,
    "code_snippet": "fn pack<T: Packed>(&mut self, t: T) -> Result<()> {\n        // safe because we build a slice of exactly size_of::<T> bytes\n        unsafe {\n            self.write_all(slice::from_raw_parts(transmute::<&T, *const u8>(&t), size_of::<T>()))?;\n        }\n        return Ok(());\n    }"
}