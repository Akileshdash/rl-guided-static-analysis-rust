{
    "level": "Info",
    "analyzer": "UnsafeDataflow",
    "op_type": "Transmute",
    "description": "Potential unsafe dataflow issue in `<de::impls::<impl de::Deserialize for alloc::boxed::Box<T>>::begin::Place<alloc::boxed::Box<T>> as de::Visitor>::map`",
    "file": "miniserde-0.1.42/src/de/impls.rs",
    "start_line": 222,
    "start_col": 13,
    "end_line": 230,
    "end_col": 14,
    "code_snippet": "fn map(&mut self) -> Result<Box<dyn Map + '_>> {\n                let mut value = NonuniqueBox::new(None);\n                let ptr = unsafe { extend_lifetime!(&mut *value as &mut Option<T>) };\n                Ok(Box::new(BoxMap {\n                    out: &mut self.out,\n                    value,\n                    map: ManuallyDrop::new(Deserialize::begin(ptr).map()?),\n                }))\n            }"
}