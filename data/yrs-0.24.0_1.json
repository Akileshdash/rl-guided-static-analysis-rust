{
    "level": "Warning",
    "analyzer": "UnsafeDestructor",
    "op_type": null,
    "description": "unsafe block detected in drop",
    "file": "yrs-0.24.0/src/atomic.rs",
    "start_line": 140,
    "start_col": 1,
    "end_line": 140,
    "end_col": 30,
    "code_snippet": "impl<T> Drop for AtomicRef<T> {\n    fn drop(&mut self) {\n        unsafe {\n            let ptr = self.0.load(Ordering::Acquire);\n            if !ptr.is_null() {\n                Arc::decrement_strong_count(ptr);\n            }\n        }\n    }\n}"
}