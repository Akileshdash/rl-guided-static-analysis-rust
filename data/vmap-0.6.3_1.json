{
    "level": "Warning",
    "analyzer": "UnsafeDestructor",
    "op_type": null,
    "description": "unsafe block detected in drop",
    "file": "vmap-0.6.3/src/map.rs",
    "start_line": 409,
    "start_col": 1,
    "end_line": 409,
    "end_col": 21,
    "code_snippet": "impl Drop for MapMut {\n    fn drop(&mut self) {\n        unsafe {\n            if self.len > 0 {\n                let (ptr, len) = Size::alloc().bounds(self.ptr, self.len);\n                unmap(ptr, len).unwrap_or_default();\n            }\n        }\n    }\n}"
}