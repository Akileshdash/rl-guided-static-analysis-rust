{
    "level": "Warning",
    "analyzer": "UnsafeDataflow",
    "op_type": "ReadFlow",
    "description": "Potential unsafe dataflow issue in `result::Result::<Ok, Err>::match_owned_ctx`",
    "file": "stabby-abi-72.1.1/src/result.rs",
    "start_line": 370,
    "start_col": 5,
    "end_line": 387,
    "end_col": 6,
    "code_snippet": "pub fn match_owned_ctx<U, T, FnOk: FnOnce(T, Ok) -> U, FnErr: FnOnce(T, Err) -> U>(\n        self,\n        ctx: T,\n        ok: FnOk,\n        err: FnErr,\n    ) -> U {\n        let is_ok = self.is_ok();\n        let storage = &self.storage;\n        if is_ok {\n            let t = unsafe { core::ptr::read(Self::ok_ptr(storage)) };\n            core::mem::forget(self);\n            ok(ctx, t)\n        } else {\n            let t = unsafe { core::ptr::read(Self::err_ptr(storage)) };\n            core::mem::forget(self);\n            err(ctx, t)\n        }\n    }"
}