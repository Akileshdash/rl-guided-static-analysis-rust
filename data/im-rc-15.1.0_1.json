{
    "level": "Info",
    "analyzer": "UnsafeDataflow",
    "op_type": "Transmute",
    "description": "Potential unsafe dataflow issue in `vector::focus::FocusMut::<'a, A>::pair`",
    "file": "im-rc-15.1.0/src/vector/focus.rs",
    "start_line": 564,
    "start_col": 5,
    "end_line": 574,
    "end_col": 6,
    "code_snippet": "pub fn pair<F, B>(&mut self, a: usize, b: usize, mut f: F) -> B\n    where\n        F: FnMut(&mut A, &mut A) -> B,\n    {\n        if a == b {\n            panic!(\"vector::FocusMut::pair: indices cannot be equal!\");\n        }\n        let pa: *mut A = self.index_mut(a);\n        let pb: *mut A = self.index_mut(b);\n        unsafe { f(&mut *pa, &mut *pb) }\n    }"
}