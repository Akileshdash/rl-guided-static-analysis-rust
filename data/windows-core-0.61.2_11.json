{
    "level": "Info",
    "analyzer": "UnsafeDataflow",
    "op_type": "Transmute",
    "description": "Potential unsafe dataflow issue in `unknown::IUnknown_Vtbl::new::AddRef`",
    "file": "windows-core-0.61.2/src/unknown.rs",
    "start_line": 165,
    "start_col": 9,
    "end_line": 172,
    "end_col": 10,
    "code_snippet": "unsafe extern \"system\" fn AddRef<T: IUnknownImpl, const OFFSET: isize>(\n            this: *mut c_void,\n        ) -> u32 {\n            unsafe {\n                let this = (this as *mut *mut c_void).offset(OFFSET) as *mut T;\n                (*this).AddRef()\n            }\n        }"
}