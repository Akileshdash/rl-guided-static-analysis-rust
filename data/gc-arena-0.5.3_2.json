{
    "level": "Info",
    "analyzer": "UnsafeDataflow",
    "op_type": "Transmute",
    "description": "Potential unsafe dataflow issue in `arena::MarkedArena::<'a, R>::finalize`",
    "file": "gc-arena-0.5.3/src/arena.rs",
    "start_line": 327,
    "start_col": 5,
    "end_line": 337,
    "end_col": 6,
    "code_snippet": "pub fn finalize<F, T>(self, f: F) -> T\n    where\n        F: for<'gc> FnOnce(&'gc Finalization<'gc>, &'gc Root<'gc, R>) -> T,\n    {\n        unsafe {\n            let mc: &'static Finalization<'_> =\n                &*(self.0.context.finalization_context() as *const _);\n            let root: &'static Root<'_, R> = &*(&self.0.root as *const _);\n            f(mc, root)\n        }\n    }"
}