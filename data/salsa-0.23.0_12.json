{
    "level": "Info",
    "analyzer": "UnsafeDataflow",
    "op_type": "Transmute",
    "description": "Potential unsafe dataflow issue in `<rayon::iter::Either<L, R> as update::Update>::maybe_update`",
    "file": "salsa-0.23.0/src/update.rs",
    "start_line": 449,
    "start_col": 5,
    "end_line": 459,
    "end_col": 6,
    "code_snippet": "unsafe fn maybe_update(old_pointer: *mut Self, new_value: Self) -> bool {\n        let old_value = unsafe { &mut *old_pointer };\n        match (old_value, new_value) {\n            (Either::Left(old), Either::Left(new)) => unsafe { L::maybe_update(old, new) },\n            (Either::Right(old), Either::Right(new)) => unsafe { R::maybe_update(old, new) },\n            (old_value, new_value) => {\n                *old_value = new_value;\n                true\n            }\n        }\n    }"
}