{
    "level": "Warning",
    "analyzer": "UnsafeDestructor",
    "op_type": null,
    "description": "unsafe block detected in drop",
    "file": "hashlink-0.10.0/src/linked_hash_map.rs",
    "start_line": 2336,
    "start_col": 1,
    "end_line": 2336,
    "end_col": 49,
    "code_snippet": "impl<K, V> Drop for DropFilteredValues<'_, K, V> {\n    fn drop(&mut self) {\n        unsafe {\n            let end_free = self.cur_free;\n            while self.cur_free != *self.free {\n                let cur_free = self.cur_free.as_ptr();\n                (*cur_free).take_entry();\n                self.cur_free = (*cur_free).links.free.next;\n            }\n            *self.free = end_free;\n        }\n    }\n}"
}