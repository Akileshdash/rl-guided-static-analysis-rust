{
    "level": "Warning",
    "analyzer": "UnsafeDataflow",
    "op_type": "ReadFlow",
    "description": "Potential unsafe dataflow issue in `collections::ptr_slice::PtrSlice::<T>::remove`",
    "file": "glib-0.21.0/src/collections/ptr_slice.rs",
    "start_line": 862,
    "start_col": 5,
    "end_line": 879,
    "end_col": 6,
    "code_snippet": "pub fn remove(&mut self, index: usize) -> T {\n        assert!(index < self.len);\n\n        unsafe {\n            let p = self.ptr.as_ptr().add(index);\n            let item = ptr::read(p as *mut T);\n            ptr::copy(p.add(1), p, self.len - index - 1);\n\n            self.len -= 1;\n\n            ptr::write(\n                self.ptr.as_ptr().add(self.len),\n                Ptr::from(ptr::null_mut::<<T as GlibPtrDefault>::GlibType>()),\n            );\n\n            item\n        }\n    }"
}