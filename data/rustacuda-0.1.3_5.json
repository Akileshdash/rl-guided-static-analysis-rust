{
    "level": "Warning",
    "analyzer": "UnsafeDestructor",
    "op_type": null,
    "description": "unsafe block detected in drop",
    "file": "rustacuda-0.1.3/src/memory/unified.rs",
    "start_line": 578,
    "start_col": 1,
    "end_line": 578,
    "end_col": 46,
    "code_snippet": "impl<T: DeviceCopy> Drop for UnifiedBuffer<T> {\n    fn drop(&mut self) {\n        if self.buf.is_null() {\n            return;\n        }\n\n        if self.capacity > 0 && mem::size_of::<T>() > 0 {\n            // No choice but to panic if this fails.\n            unsafe {\n                let ptr = mem::replace(&mut self.buf, UnifiedPointer::null());\n                cuda_free_unified(ptr).expect(\"Failed to deallocate CUDA unified memory.\");\n            }\n        }\n        self.capacity = 0;\n    }\n}"
}